type themeSettings = {
  appBarColor: string,
  abTextColor: string,
  activeButtonColor: string,
};

const settings: themeSettings = {
  appBarColor: '#00630f',
  abTextColor: '#efefef',
  activeButtonColor: '#00ff28',
};

export default settings;
